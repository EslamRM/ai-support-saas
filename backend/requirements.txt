# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Database ---
sqlalchemy==2.0.35
alembic==1.13.2
psycopg2-binary==2.9.9
asyncpg==0.29.0

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib==1.7.4
# Pinned explicitly (not left to passlib[bcrypt]'s resolver): passlib 1.7.4
# breaks against bcrypt>=4.1, which removed the __about__ attribute
# passlib's backend version-detection reads. Caught by actually running
# the Phase 3 test suite -- see docs/troubleshooting.md.
bcrypt==4.0.1
email-validator==2.2.0
python-multipart==0.0.9

# --- Background jobs ---
celery[redis]==5.4.0
redis==5.0.8

# --- AI / RAG ---
openai==1.51.0
langgraph==0.2.28
langchain-core==0.3.6
qdrant-client==1.19.1
tiktoken==0.8.0

# --- Document processing ---
pypdf==5.0.1
python-docx==1.1.2

# --- Observability ---
structlog==24.4.0

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
